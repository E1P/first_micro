# Express with Deta Demo Project

NODE_ENV set as 'development' with start script.

```bash
NODE_ENV=development nodemon index.js
```

NODE_ENV set on server as 'production' via '.env' file.

```bash
deta update -e .env
```
